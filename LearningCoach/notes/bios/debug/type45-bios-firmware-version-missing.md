# SMBIOS Type45 missing BIOS firmware version entry

Date: 2026-05-25

Status: verified fixed. The final generated token value had been `FIRMWARE_INVENTORY_INFO = 0`, so the BIOS Firmware Type45 static record was compiled out. Enabling it made the BIOS Firmware Type45 record appear.

## Symptom

`dmidecode -t 45` shows Type45 Firmware Inventory records for:

- `BMC Firmware`, with version such as `0.13`
- `TPM Firmware`, with version such as `70039.24078912`

But there is no visible `BIOS Firmware` Type45 record / BIOS version information.

## Verification Result

After enabling the BIOS Type45 static record, `dmidecode -t 45` shows three Firmware Inventory records:

- `BIOS Firmware`
- `BMC Firmware`
- `TPM Firmware`

The verified BIOS Type45 record includes:

```text
Firmware Component Name: BIOS Firmware
Firmware Version: 0.01.01
Release Date: 05/25/2026
Lowest Supported Firmware Version: 0.01.01
Image Size: 58 MB on the later checked build
State: Enabled
Associated Components: 1
  0x0000
```

This confirms the root cause was the static BIOS Type45 record being disabled, not a `dmidecode` parsing issue and not a BMC/TPM dynamic Type45 issue.

Later field review added one important nuance: the BIOS Type45 record is not purely static. It is created from static SMBIOS data first, then `DynamicUpdateBiosFirmwareInfo()` patches only a subset of fields at runtime.

## Key Code Paths

BMC Type45 is generated dynamically here:

```text
/Users/tzy/code/personal-coach/code-repositories/tririverv5-ami/AmiIpmi2Pkg/Ipmi2/IpmiInitialize/IpmiSmbiosTypes.c
```

Relevant functions:

- `UpdateSmbiosType45Record()` builds a Type45 record with `FirmwareComponentName = "BMC Firmware"`.
- It is called only after Type38 succeeds and `IPMI_SMBIOS_TYPE45_SUPPORT` is enabled.

TPM Type45 is generated dynamically here:

```text
/Users/tzy/code/personal-coach/code-repositories/tririverv5-ami/AmiModulePkg/TCG2/Common/TpmSmbios/TpmSmbiosDxe.c
```

Relevant function:

- `UpdateSmbiosType45TpmRecord()` builds a Type45 record with `FirmwareComponentName = "TPM Firmware"`.

BIOS Type45 is expected to come from AMI static SMBIOS data here:

```text
/Users/tzy/code/personal-coach/code-repositories/tririverv5-ami/AmiCompatibilityPkg/Smbios/SmbiosBoard/SmbiosStaticDataDt/SmbiosStaticData.dt
/Users/tzy/code/personal-coach/code-repositories/tririverv5-ami/AmiCompatibilityPkg/Smbios/SmbiosBoard/SmbiosStaticDataDt/SmbiosStaticDataDt.sdl
/Users/tzy/code/personal-coach/code-repositories/tririverv5-ami/AmiCompatibilityPkg/Smbios/SmbiosBoard/SmbiosBoard.sdl
```

Important static data logic:

```text
SmbiosStaticData.dt:
  #if TYPE45_STRUCTURE
    Type 45 record is emitted
    BIOS_FW_COMPONENT_NAME -> "BIOS Firmware"
    BIOS_FW_VERSION        -> $(BIOS_VERSION)
  #endif
```

Important token defaults:

```text
SmbiosStaticDataDt.sdl:
  FIRMWARE_INVENTORY_INFO = 1
  TYPE45_STRUCTURE        = $(FIRMWARE_INVENTORY_INFO)
  BIOS_FW_COMPONENT_NAME  = "BIOS Firmware"
  BIOS_FW_VERSION         = $(BIOS_VERSION)

TencentLegoPkg.sdl:
  BIOS_VERSION            = $(BIOS_TAG)
```

But this platform override exists:

```text
AmiCrbPlatformPkg/ModuleOverrides/ModuleOverrides.sdl:
  FIRMWARE_INVENTORY_INFO = 0
```

## Current Best Guess

Confirmed cause:

> The BIOS Type45 static record is being compiled out because the platform override sets `FIRMWARE_INVENTORY_INFO` to `0`, which makes `TYPE45_STRUCTURE` evaluate to disabled for the static SMBIOS table.

This matches the observed result: BMC and TPM Type45 records still appear because they are generated by their own dynamic drivers, independent of the AMI static BIOS Type45 table. The missing part is specifically the static `BIOS Firmware` entry.

## How We Located It Quickly

The key was to avoid treating "Type45 not showing BIOS version" as a generic SMBIOS parsing problem. The screenshot already gave a strong clue:

- `dmidecode -t 45` showed `BMC Firmware`.
- `dmidecode -t 45` showed `TPM Firmware`.
- It did not show `BIOS Firmware`.

That means Type45 itself was not globally broken. At least two Type45 producers were working. So the useful question became:

> Which code path produces each Type45 record?

The search then split Type45 into three producers:

1. BMC Type45: dynamic record from `AmiIpmi2Pkg/Ipmi2/IpmiInitialize/IpmiSmbiosTypes.c`
2. TPM Type45: dynamic record from `AmiModulePkg/TCG2/Common/TpmSmbios/TpmSmbiosDxe.c`
3. BIOS Type45: static SMBIOS table from `AmiCompatibilityPkg/Smbios/SmbiosBoard/SmbiosStaticDataDt/SmbiosStaticData.dt`

After that split, the bug shape became clear:

- BMC/TPM records appearing proves the dynamic paths are alive.
- BIOS record missing points at the static SMBIOS table path.
- The static BIOS Type45 block is guarded by `#if TYPE45_STRUCTURE`.
- `TYPE45_STRUCTURE` comes from `FIRMWARE_INVENTORY_INFO`.
- The default SDL says `FIRMWARE_INVENTORY_INFO = 1`.
- The platform override says `FIRMWARE_INVENTORY_INFO = 0`.

So the fast path was:

```text
Runtime symptom
  -> distinguish which Type45 records exist
  -> map existing records to dynamic producers
  -> infer missing BIOS record must be static table path
  -> inspect static table guard macro
  -> trace token default to platform override
  -> confirm final generated token is 0
```

This is a useful repeatable pattern for BIOS SMBIOS bugs:

1. First decide whether the table type is globally missing or only one producer is missing.
2. Search for the displayed component names, not only the SMBIOS type number.
3. Separate static SMBIOS table generation from dynamic `SmbiosProtocol->Add()` paths.
4. For static records, always trace SDL/token override order instead of trusting the first default value seen in a package.

## Field Source Review

The useful mental model is:

```text
TYPE45_STRUCTURE token enables the BIOS Type45 static table
  -> SmbiosStaticData.dt emits the BIOS Firmware Type45 record
  -> Smbios.c / DynamicUpdateBiosFirmwareInfo() patches selected fields
```

The dynamic update path is real, but it does not own every field.

Relevant runtime call:

```text
/Users/tzy/code/personal-coach/code-repositories/tririverv5-ami/AmiCompatibilityPkg/Smbios/Smbios.c
  DynamicUpdateStructures()
    #if (TYPE45_STRUCTURE && UPDATE_FIRMWARE_INVENTORY_TYPE45)
      DynamicUpdateBiosFirmwareInfo(SmbiosDataTable)
```

`DynamicUpdateBiosFirmwareInfo()` currently changes:

```text
Type45 instance 1, string 3 -> Firmware ID, from OEM_ESRT_FIRMWARE_GUID
Type45 instance 1, string 4 -> Release Date, from TODAY when UPDATE_BIOS_RELEASE_DATE is enabled
Associated Component Handle -> Type0 handle
```

It does not update:

```text
Firmware Version
Manufacturer
Image Size
Lowest Supported Firmware Version
```

### BIOS Firmware Version

`Firmware Version` is generated from the static token chain:

```text
SmbiosStaticData.dt:
  BIOS_FW_VERSION_STRING_NO is the Type45 Firmware Version string number
  String: BIOS_FW_VERSION

SmbiosStaticDataDt.sdl:
  BIOS_FW_VERSION = $(BIOS_VERSION)

TencentLegoPkg.sdl:
  BIOS_VERSION = $(BIOS_TAG)
  BIOS_TAG = $(PROJECT_MAJOR_VERSION).$(PROJECT_MINOR_VERSION).$(PROJECT_SUB_VERSION)
```

There is no Type45 equivalent of:

```text
ReplaceStructureString(Buffer, 45, 1, 2, ...)
```

So for the BIOS Firmware Type45 record, `Firmware Version` is build-time token data, not runtime data from `DynamicUpdateBiosFirmwareInfo()`.

Important future risk:

- Type0 has an `UPDATE_BIOS_VERSION` path that can dynamically rewrite Type0 BIOS Version.
- Type45 BIOS Firmware Version does not follow that dynamic rewrite.
- If `UPDATE_BIOS_VERSION` is enabled later, Type0 and Type45 can drift unless Type45 is updated too or both are kept on the same static token source.

### Manufacturer

Current source:

```text
SmbiosStaticDataDt.sdl:
  DEFAULT_STRING = "Default string"
  BIOS_FW_MANUFACTURER = $(DEFAULT_STRING)

SmbiosStaticData.dt:
  Manufacturer string number points to BIOS_FW_MANUFACTURER
  String: BIOS_FW_MANUFACTURER
```

This means `dmidecode` shows `Manufacturer: Default string` because the field is explicitly filled with AMI's default placeholder. It is not an absent field.

Decision points:

- If the requirement is to show the real BIOS firmware producer, set `BIOS_FW_MANUFACTURER` to an approved real source, such as the same family as Type0 `BIOS_VENDOR`, if product requirements agree.
- If the requirement is "unknown / not specified", do not fill `"Default string"` as a placeholder. Make the string number become `0` or use the platform's approved "not filled" wording, depending on the team's SMBIOS policy.
- This requirement cannot be inferred from code alone; confirm whether Type45 Manufacturer is required and what value the product owner expects.

### Image Size

Current source:

```text
SmbiosStaticDataDt.sdl:
  BIOS_FW_IMAGE_SIZE = $(FLASH_SIZE)

BiosFdfLayout.sdl:
  FLASH_SIZE = PcdFlashFdSecPeiSize + PcdFlashFdMainSize + PcdFlashFdBinarySize

Current checked layout:
  PcdFlashFdSecPeiSize   = 0x00A00000
  PcdFlashFdMainSize     = 0x00600000
  PcdFlashFdBinarySize   = 0x02A00000
  Total                  = 0x03A00000 = 58 MB
```

This matches the separate ROM layout hint:

```text
TencentLegoPkg/Dxe/TencentAssetInfo/RomLayoutInfo.c:
  ONLY_BIOS_SIZE = 0x3A00000
```

So Type45 `Image Size: 58 MB` is plausibly the BIOS image / BIOS region size.

By contrast, Type0 `ROM Size: 64 MB` is influenced by a different path:

```text
SmbiosBoard.c:
  BIOS ROM Size (Physical Flash device) dynamic update
  GetMaxRegionLimit()
  Type0.BiosRomSize = MaxFlashLimit / 0x10000
```

So the two fields may be semantically different:

- Type0 `ROM Size`: physical SPI flash / BIOS ROM capacity.
- Type45 `Image Size`: firmware image currently programmed in the device, which can mean the BIOS region/image size.

Decision point:

- If the product requirement says Type45 Image Size must report physical ROM size, then `BIOS_FW_IMAGE_SIZE` should be changed to the same source as Type0's physical flash interpretation.
- If the product requirement says Type45 Image Size should report the BIOS firmware image/region size, then `58 MB` is defensible and may be more accurate than `64 MB`.
- Confirm this explicitly with the team; code alone can explain current value, but it cannot decide the product requirement.

### Requirement Checks To Remember

For future SMBIOS field reviews, always ask:

1. Is there a real requirement for this field to be shown?
2. If the field is shown, what exact source should it use?
3. Should Type45 BIOS Firmware Version be the same as Type0 BIOS Version?
4. Should Type45 Image Size mean BIOS region/image size or physical ROM capacity?
5. If a field is unknown, should it be omitted/string index `0`, show `Not Specified`, show `Not Filled`, or use a real default?

## How To Trace Static vs Dynamic SMBIOS Code

The practical entry point is the OS output. For Type45, `dmidecode` already exposes the SMBIOS strings:

```text
Firmware Component Name: BMC Firmware
Firmware Component Name: TPM Firmware
```

Use those visible strings as code search anchors. This is often faster than searching only for `Type45`, because one SMBIOS type can be produced by several independent modules.

Useful search pattern:

```sh
rg -n "\"BMC Firmware\"|\"TPM Firmware\"|\"BIOS Firmware\"" <bios-root>
rg -n "Type45|TYPE45|SMBIOS_TYPE_FIRMWARE_INVENTORY_INFORMATION|FIRMWARE_INVENTORY_INFO" <bios-root>
```

When the string lands in C code, inspect nearby code for these dynamic-generation signs:

```c
Type45Record->Hdr.Type = 45;
Type45Record->SmbiosType45.Hdr.Type = 45;
SmbiosProtocol->Add (..., (EFI_SMBIOS_TABLE_HEADER *) Type45Record);
```

That means the record is built at runtime by a DXE/driver path. In this case:

- `"BMC Firmware"` is a C string in `AmiIpmi2Pkg/Ipmi2/IpmiInitialize/IpmiSmbiosTypes.c`, then `UpdateSmbiosType45Record()` calls `SmbiosProtocol->Add()`.
- `"TPM Firmware"` is a C string in `AmiModulePkg/TCG2/Common/TpmSmbios/TpmSmbiosDxe.c`, then `UpdateSmbiosType45TpmRecord()` calls `SmbiosProtocol->Add()`.

When the string lands in `.dt` / `.sdl`, inspect nearby code for static-generation signs:

```text
UINT8 : 0x2D                 // Type 45
#if TYPE45_STRUCTURE
String: BIOS_FW_COMPONENT_NAME
String: BIOS_FW_VERSION
```

That means the record is generated from the static SMBIOS data table during build, not hand-built by a runtime driver. In this case:

- `"BIOS Firmware"` comes from `BIOS_FW_COMPONENT_NAME`.
- The BIOS version comes from `BIOS_FW_VERSION = $(BIOS_VERSION)`.
- The whole static Type45 block is guarded by `TYPE45_STRUCTURE`.
- `TYPE45_STRUCTURE` follows `FIRMWARE_INVENTORY_INFO`.
- A platform override turned `FIRMWARE_INVENTORY_INFO` to `0`, so this static record never entered the final SMBIOS table.

So the difference is:

```text
Dynamic Type45:
  string literal in .c
  Allocate/CopyMem/build structure
  Hdr.Type = 45
  SmbiosProtocol->Add()

Static Type45:
  raw table layout in .dt
  strings/tokens in .sdl
  guarded by SDL macros
  included or excluded at build time
```

For future SMBIOS bugs, the shortest tracing order is:

1. Copy the exact visible string from `dmidecode`.
2. Search the string first.
3. If it is in `.c`, look for `SmbiosProtocol->Add()` and the module entry/callback.
4. If it is in `.dt` / `.sdl`, trace the guard macro and all platform/project overrides.
5. Only after that search broad terms like `Type45`, because broad type searches produce too much noise.

## Secondary Suspects

If generated tokens show `TYPE45_STRUCTURE = 1`, then check these next:

1. `BIOS_VERSION` / `BIOS_TAG` may be empty or not propagated, causing a `BIOS Firmware` record with a blank or unexpected version string.
2. `DynamicUpdateBiosFirmwareInfo()` only updates firmware ID and associated handle; it does not dynamically set the BIOS version string. BIOS version is expected to already exist in the static string area as `BIOS_FW_VERSION`.
3. `DynamicUpdateBiosFirmwareInfo()` calls `FindStructureType(..., 45, 1)` and updates the first Type45 record in the static buffer. If ordering or duplicate Type45 handling changes, verify it is touching the BIOS static Type45, not a later dynamic BMC/TPM record.
4. `dmidecode 3.5` warns that SMBIOS 3.9 is newer than it fully supports. This is probably not the main issue because it still parses BMC/TPM Type45 records, but raw `dmidecode --dump-bin` can be used if output looks suspicious.

## Next Verification

On a build machine, check generated token values:

```text
TYPE45_STRUCTURE
FIRMWARE_INVENTORY_INFO
UPDATE_FIRMWARE_INVENTORY_TYPE45
BIOS_FW_VERSION
BIOS_VERSION
BIOS_TAG
```

On the target OS, compare:

```sh
dmidecode -t 0
dmidecode -t 45
dmidecode --dump-bin /tmp/smbios.bin
```

Expected good Type45 shape:

```text
Firmware Component Name: BIOS Firmware
Firmware Version: <same family as BIOS_TAG / Type0 BIOS Version>
Associated Components: 1
  <Type0 BIOS Information handle>
```

## Suggested Fix Direction

Applied fix direction:

- Enable `FIRMWARE_INVENTORY_INFO` / `TYPE45_STRUCTURE` for this platform path, or add a project override that forces the BIOS Type45 static record into the SMBIOS static table.

Verification checklist:

- `dmidecode -t 45` has three records: BIOS Firmware, BMC Firmware, TPM Firmware.
- BIOS Type45 `Firmware Version` matches the expected `BIOS_TAG` or Type0 BIOS version.
- BIOS Type45 associated component handle points to Type0, not Type38/Type43.
