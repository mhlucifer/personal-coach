# Foundation Exam Bank

## Struct And Bitfield Starter Set

1. Concept: What is the difference between a struct field and a pointer to a struct?
2. Code trace: Given `PackedValue = (High << 3) | Low`, identify which bits store each value.
3. Modification risk: What happens if a packed field is written with the wrong width?
4. Debug: How would you print a struct field before and after assignment?
5. Boundary: What if `AllocateZeroPool` returns `NULL`?
